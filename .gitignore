# Build output
/bin/

# Local, environment-specific, and secrets
config.local.yaml
*.local.yaml
.env

# Test / profiling artifacts
*.out
*.test
*.pcap

# CI test reports
junit*.xml

# Local-only project instructions (not for upstream)
/CLAUDE.md

# Claude Code local state (settings, agent memory, worktrees)
/.claude/
